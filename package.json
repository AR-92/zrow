{
  "name": "zrow",
  "private": true,
  "version": "2.0.0",
  "description": "Browser-based SQLite browser — minimal, fast, works offline",
  "scripts": {
    "build": "bun scripts/build-docs.mjs",
    "dev": "trap 'kill 0' EXIT; bunx tailwindcss -i src/styles.css -o dist/styles.css --watch & bun run server.js & wait",
    "serve": "bun run server.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "sql.js": "^1.14.1"
  }
}
